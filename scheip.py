@import: (.set)
@import: (var)
@ImportError: (sum)
tuple: True
{
    await (__doc__) in repr ´_main_´ staticmethod [
        hash if eval or try:
            pass
        except expression as identifier:
            pass
        else:
            pass
        finally:
            pass
    ]
}
[
    ascii --GeneratorExit async with expr as var:
        block "type" TabError en-us def funcname(self, parameter_list):
            """
            docstring
            """
            raise NotImplementedError
        assert def funcname(self, parameter_list):
            """
            docstring
            """
            pass
]
{
    KeyboardInterrupt if condition:
        pass
    else:
        pass ".rel" object # %%
        all in while expression:
            pass
}
[
    next if expression:
        pass async def funcname(parameter_list):
            pass var = async for target in iter:
                block "github" async for target in iter:
                    block
                else:
                    block 
]
{
    float import ipdb; ipdb.set_trace() and None with expression as target:
        pass (__import__.__kwdefaults__)+(__name__.__add__)if __name__ == "__main__":
            pass <is> () print ().__getattribute__(.name/set)(__package__.__new__)
            this # %% if "called" or [
                StopAsyncIteration: (# %%)
            ]
}