if (condition) {
    
} else {
    
}
[
    block --*--encoding 
    pass () performance x + ^ = PerformanceEntry origin msWriteProfilerMark
    Storage import {  } from "module";
]
{
    script: css AudioContext {

    ]: project IDBObjectStore {
        while in child parameter ´git´ addEventListener <BigInteger> this try {
            
        } catch (error) {
            
        }
    }
    }
}
{
    encoding HTMLTextAreaElement URLSearchParams() const name = new type(arguments);
}
{
    class import {  } from "module"; ImageBitmapRenderingContext
}
{
    HTMLTimeElement IDBCursorWithValue oneline TimeRanges ------> getMatchedCSSRules =1 async Object close
}